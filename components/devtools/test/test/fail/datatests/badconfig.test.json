{
  "description": "Bad test configuration",
  "tests": [
    {
      "description": "Expect error and result",
      "calls": [
        {
          "op": "getValue",
          "expect": "expect(value).to.equal(6)",
          "expectError": "expect(error).to.be.not.undefined"
        }
      ]
    },
    {
      "description": "Use illegal op",
      "calls": [
        {
          "op": "foo",
          "expect": "expect(value).to.equal(6)"
        }
      ]
    }
  ]
}
